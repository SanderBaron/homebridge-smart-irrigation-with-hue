/**
 * Static identifiers shared between the plugin entry point, the platform class,
 * and the custom UI server. These must match the `pluginAlias` in
 * `config.schema.json` and the `name` field in `package.json`.
 */

export const PLATFORM_NAME = 'SmartIrrigation';
export const PLUGIN_NAME = 'homebridge-weather-smart-irrigation';
